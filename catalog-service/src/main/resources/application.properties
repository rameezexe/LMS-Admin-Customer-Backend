# ── Server ──
server.port=8087
spring.application.name=catalog-service

# ── Eureka ──
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# ── H2 Datasource ──
spring.datasource.url=jdbc:h2:file:./data/catalogDB;AUTO_SERVER=TRUE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# H2 web console at /h2-console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ── JWT ──
app.jwt.secret=dGhpc0lzQVZlcnlTZWN1cmVTZWNyZXRLZXlGb3JKV1RBdXRoMjU2Qml0cw==
app.jwt.expiration=86400000

# ── Swagger ──
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# ── Actuator ──
management.endpoints.web.exposure.include=health,info

# ── AWS S3 ──
aws.s3.bucket-name=lms-tcs
aws.s3.region=eu-north-1
aws.s3.access-key=AKIAUQTPFAGAS3OEJRCR
aws.s3.secret-key=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
aws.s3.base-url=https://${aws.s3.bucket-name}.s3.${aws.s3.region}.amazonaws.com

# ── Multipart Upload ──
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=6MB
